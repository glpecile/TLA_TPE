codigo:
numero a;
numero b;
numero aux;

imprimir("CALCULADORA DE MCD\n");
leer(a);
leer(b);


si se cumple ( b vale mas que 0 ) {
    hacer{
        aux vale b;
        b vale (a modulo b);
        a vale aux;
    }mientras(b vale mas que 0);
}

imprimir(a);